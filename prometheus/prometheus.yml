global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "rules.yml"

alerting:
  alertmanagers:
    - static_configs:
        - targets: []

scrape_configs:
  - job_name: "error-budget-poc"
    static_configs:
      - targets: ["app:8000"]
    metrics_path: /metrics
